Auto-generated by opencode-meta — 2026-04-21T00:00:00Z

High-signal instructions for agents working in this repository.

Keep it short. Each line answers: "Would an agent likely miss this without help?" If not, it is omitted.

Core facts
- This is a Maven multi-module project with two modules: basisprojekt-client and basisprojekt-server (see root pom.xml modules list).
- Preferred full-build (correct module order + checks): run from repo root: `mvn clean install`.

Frontend (basisprojekt-client)
- Client is a Vite + React + TypeScript app with npm scripts in basisprojekt-client/package.json.
- Typical frontend commands (from repo root):
  - `cd basisprojekt-client && npm ci` (install reproducibly)
  - `cd basisprojekt-client && npm run build` (tsc -b then vite build)
  - `cd basisprojekt-client && npm run test` (runs vitest)
- Node version hint: .nvmrc exists — prefer matching that when running local installs.
- The built client artifact lands in basisprojekt-client/dist and is packaged into the server artifact (server depends on client). Building client separately is fine for local UI work but use root `mvn` for full integration builds.

Backend (basisprojekt-server)
- Server is Spring Boot (Java 21). Prefer `mvn` from repo root; server also contains a Maven wrapper at basisprojekt-server/mvnw if you need to build server only.
- Exact server-only build: `./basisprojekt-server/mvnw -f basisprojekt-server/pom.xml clean install` (or use `mvn -pl basisprojekt-server -am install`).
- Code style is enforced during Maven validate: Spotless and Checkstyle run and are configured to fail the build (failsOnError=true). Don't skip these unless explicitly asked.
- License/header requirement: Spotless uses src/main/checkstyle/java.header.plain — preserve header if editing Java files.

Build & test ordering gotchas
- Root `mvn clean install` is the reproducible path CI uses and respects module ordering (client -> server). Use it for changes that touch both modules or to reproduce CI failures.
- If you only change frontend code, you can run client npm commands, but the server's final artifact served by Docker will expect the client dist to be packaged (so root mvn needed for full end-to-end verification).

CI / Integration tests
- CI (.gitlab-ci.yml) runs `mvn clean install` then an integration stage using docker-compose and Docker-in-Docker (docker:dind). CI sets DOCKER_HOST="tcp://docker:2375" and disables TLS for DinD.
- Testcontainers in server tests assume Docker available. Locally, ensure Docker is running; reproducing CI integration often requires `docker-compose up -d` from repo root and then `curl http://localhost:8080/` and `curl http://localhost:8080/api/customers`.
- CI curl checks use the docker network hostname `docker:8080` — locally use `localhost:8080` unless reproducing within a container network.

Tests and expensive suites
- Unit tests: server uses JUnit/Testcontainers; client uses Vitest. Run locally per-module as above.
- Integration tests may require Docker and can be slow; don't run the full integration stage by default in short dev cycles.

Where to look for API surface and wiring
- Server REST controllers and DTOs: basisprojekt-server/src/main/java/de/openknowledge/baseproject/customer/web/CustomerController.java and CustomerResponse.java.
- Client API calls use `/api/customers` (basisprojekt-client/src/features/customers/api/customerApi.ts). Ensure endpoint paths and shapes match these files when changing API or schemas.

Files and directories an agent should not modify automatically
- Do not modify generated/build artifacts under `*/target/` or `basisprojekt-client/dist/`.
- Avoid changing CI files (.gitlab-ci.yml) or publishing credentials/tokens unless explicitly instructed.

Quick troubleshooting checks an agent should run first
- If a Maven build fails: inspect `basisprojekt-server/target/surefire-reports/` and `target/checkstyle-result.xml` for failing tests or style violations.
- If frontend tests fail: run `cd basisprojekt-client && npm run test -- --reporter=verbose` and check `basisprojekt-client/src/test/setup.ts` for test harness issues.

If something is unclear
- Ask one question: whether it's acceptable to run Docker/Testcontainers locally, or whether long-running integration suites are allowed. (Use only one concise question.)

References (executable sources of truth)
- Root pom.xml (module order)
- basisprojekt-client/package.json (npm scripts)
- basisprojekt-server/pom.xml (spotless/checkstyle & testcontainers)
- .gitlab-ci.yml (how CI reproduces integration tests)

Agent workflow for feature implementation
- **Specifier**: Creates a structured feature specification answering who/what/how/test.
- **Architect**: Refines specification with technical decisions, validates architecture, provides implementation plan detailing backend APIs, database changes, frontend components, and integration points.
- **Backend**: Reads specification and implementation plan; implements backend services/repositories/controllers and writes unit/integration tests.
- **Frontend**: Reads specification and implementation plan; implements components, pages, API integration, and unit tests.
- **Testing**: Validates full integration between backend and frontend; runs end-to-end tests with docker-compose.
- **Reviewing (1st pass)**: Reviews all changes from Backend, Frontend agents against code quality, best practices, and architectural consistency.
- **Refactor**: Analyzes code for simplification opportunities; refactors to reduce complexity while maintaining all test coverage.
- **Reviewing (2nd pass)**: Reviews refactoring changes to ensure quality, no regressions, and compliance with standards.

End of file.

Repository meta-agent policy
- Whenever AGENTS.md, opencode.json (root), or any file under `.opencode/` is modified, invoke the `opencode-meta` agent to regenerate and validate agent metadata and instructions.
- The `opencode-meta` run must produce: a) the updated `.opencode/opencode.json` (if applicable), b) a short changelog of derived changes, and c) verification steps it ran. Do not auto-merge those results; present the patch and verification output for human review.
