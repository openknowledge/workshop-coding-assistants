# Copy to `.env` and fill in one of the provider keys before running
# `docker compose -f docker-compose.opencode.yaml up`.
# Alternatively, leave empty and run `opencode auth login` inside the container.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=
GROQ_API_KEY=
